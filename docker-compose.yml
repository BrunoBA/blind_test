version: '2'
services: 
  frontend:
    build:
      context: .
      dockerfile: ./docker/nodejs/Dockerfile
    container_name: frontend_container
    ports:
      - 3000:3000
    volumes: 
      - .:/app/home
    tty: true